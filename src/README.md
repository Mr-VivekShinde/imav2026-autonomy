# ROS 2 workspace source directory

Phase 0 intentionally contains no migrated autonomy package yet.

Packages from the previous Humble repository will be ported one by one to Jazzy instead of copied blindly. This keeps the workspace buildable and makes regressions attributable.
